# Firebase設定チェックリスト

## ✅ 正しい設定の見分け方

### 正しい設定の例：
```javascript
window.firebaseConfig = {
    apiKey: "AIzaSyAbCdEfGhIjKlMnOpQrStUvWxYz1234567",  // "AIzaSy"で始まる長い文字列
    authDomain: "my-project-12345.firebaseapp.com",     // "プロジェクトID.firebaseapp.com"
    projectId: "my-project-12345",                      // プロジェクトID（英数字とハイフン）
    storageBucket: "my-project-12345.appspot.com",     // "プロジェクトID.appspot.com"
    messagingSenderId: "123456789012",                   // 数字の文字列（12桁程度）
    appId: "1:123456789012:web:abcdef1234567890"        // "1:数字:web:文字列"の形式
};
```

### ❌ 間違っている設定の例：
```javascript
window.firebaseConfig = {
    apiKey: "YOUR_API_KEY",                    // ← まだプレースホルダー
    authDomain: "YOUR_PROJECT_ID.firebaseapp.com",  // ← まだプレースホルダー
    projectId: "YOUR_PROJECT_ID",              // ← まだプレースホルダー
    // ...
};
```

## チェックポイント

- [ ] `apiKey` が "AIzaSy" で始まっている
- [ ] `authDomain` が "プロジェクトID.firebaseapp.com" の形式になっている
- [ ] `projectId` が実際のプロジェクトIDになっている（"YOUR_PROJECT_ID"ではない）
- [ ] `storageBucket` が "プロジェクトID.appspot.com" の形式になっている
- [ ] `messagingSenderId` が数字の文字列になっている
- [ ] `appId` が "1:数字:web:文字列" の形式になっている
- [ ] `window.firebaseConfig =` になっている（`const firebaseConfig =` ではない）

## 現在の設定を確認する方法

1. `firebase-config.js` ファイルを開く
2. 上記のチェックポイントと照らし合わせる
3. すべての項目が実際の値になっていればOK！
