# firebase-config.js の編集手順

## 📝 ステップバイステップ

### 1. ファイルを開く
- Cursorの左側のファイル一覧で `firebase-config.js` をクリック
- または、Ctrl+P（Mac: Cmd+P）を押して「firebase-config.js」と入力してEnter

### 2. Firebase Consoleから設定をコピー
Firebase Consoleで表示された設定コードをコピー：
```javascript
const firebaseConfig = {
  apiKey: "AIzaSy...",
  authDomain: "...",
  // ...
};
```

### 3. ファイルを編集
1. 8行目から15行目までを選択（マウスでドラッグ、またはShift+矢印キー）
2. Deleteキーで削除
3. コピーした設定を貼り付け（Ctrl+V / Cmd+V）

### 4. 修正
貼り付けたコードの最初の行を変更：
- **変更前**: `const firebaseConfig = {`
- **変更後**: `window.firebaseConfig = {`

### 5. 保存
- Ctrl+S（Mac: Cmd+S）で保存

## ✅ 完成例

```javascript
// Firebase設定
// このファイルにFirebaseプロジェクトの設定を追加してください
// Firebase Console (https://console.firebase.google.com/) から取得できます

// 注意: このファイルは公開リポジトリに含めることができますが、
// 本番環境ではFirebase Security Rulesで適切に保護してください

window.firebaseConfig = {
    apiKey: "AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXXX",
    authDomain: "your-project-id.firebaseapp.com",
    projectId: "your-project-id",
    storageBucket: "your-project-id.appspot.com",
    messagingSenderId: "123456789012",
    appId: "1:123456789012:web:abcdef1234567890"
};
```

## 🎯 ポイント
- `const firebaseConfig =` → `window.firebaseConfig =` に変更するのを忘れずに！
- 引用符（""）はそのまま残す
- カンマ（,）もそのまま残す
